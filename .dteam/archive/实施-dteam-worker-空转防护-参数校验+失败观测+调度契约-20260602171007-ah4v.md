# 实施 dteam worker 空转防护：参数校验+失败观测+调度契约

## 基本信息- ID: 20260602171007-ah4v
- 类型: infra
- 创建时间: 2026-06-02T09:10:07.919Z
- 状态: InProgress

## 目标
- 为什么: 设计已收敛（task 20260602170625-tc2t）：需要把“fail-fast + 结构化失败 + 最小观测键 + 调度/编排契约”落地到代码和测试，彻底消除 worker_start 快速 failed 且无诊断上下文的空转问题。
- 做什么: 按设计方案实施并验证：1) 在 worker_create/worker_start 增加必填参数校验与 role/model/sessionModel 三级解析记录；2) 失败路径输出结构化错误并写入共享内存；3) 收紧 dteam(plan/run) 与 chain plan 的输入输出契约；4) 新增回归测试并运行 npm test 全绿；5) 同步相关 README/模块说明。

## 范围- 包含:
  - worker_create/worker_start 参数校验与结构化失败输出
  - role/model/sessionModel 三级解析与诊断记录
  - dteam(plan/run) 与 chain plan 输入输出契约收紧
  - 共享内存最小观测键写入（status/lastPhase/lastError/lastModel）
  - 最小回归测试（缺参数、模型解析失败、空 step）
  - 变更相关 README/模块说明同步
- 排除:
  - 重写 worker 架构
  - 新增角色/新编排模式
  - LLM 供应商账号/密钥排障
  - 与空转防护无关的 UI 大改

## 验收条件（GWT + 测试）- [x] AC1 缺参数失败：调用 worker_create/worker_start 缺少关键参数时立即返回可解析错误，且共享内存写入诊断键
- [x] AC2 模型解析失败：role/model/sessionModel 三级解析失败时返回明确错误，不进入空执行
- [x] AC3 空 step 拒绝：dteam(plan/run) 与 chain plan 产出空任务描述/空角色时拒绝执行并提示修正
- [x] AC4 集成测试：新增覆盖以上三类场景的最小测试，`npm test` 全绿
- [x] AC5 文档同步：README/模块说明更新，保持契约与实现一致
- [x] AC6 可复现验证：给出手动验收命令与预期输出，后续可直接执行验证

## 阶段记录### 探索发现
- 本任务承接设计任务 `20260602170625-tc2t` 的结论：当前问题不是链路全缺，而是"契约薄弱 + 失败不解释"。
- 代码关键链路仍在：`src/P2/worker.ts`（worker create/start 与默认 executor/spawnAgent）、`src/P3/worker.ts`（runWorker）、`src/P4/index.ts`（wrapWorker + llm executor）、`src/P4/dteamTool.ts`（dteam 调度入口）。
- 现有测试已全绿（19 文件 / 258 测试），说明当前回归更像"运行时缺参数/缺诊断"类问题，需要补齐输入校验与失败写入，而不是立刻重写架构。
- 共享内存的 getMemory 返回空键，说明现有实现对失败分支的上下文沉淀不足，必须新增最小观测键。

### 讨论决策
- 按设计方案（D1~D6）实施：fail-fast + 结构化错误 + 三级解析记录 + 调度契约 + 最小观测键 + 三类回归测试。
- 实施原则：先做最小侵入修改，优先保证"失败可解释、可复现、可验证"，再考虑更复杂的改进。

### 执行记录（阶段性）
- 已在 `src/P2/worker.ts` 补充 `worker-<id>` 共享内存诊断键：启动写入 startedAt/lastPhase/config；失败写入 lastError/endedAt；成功写入 endedAt。
- 已在 `src/P3/chainPlanner.ts` 收紧校验：`generatePlan` 在缺少「目标→做什么」时抛错，避免产出空 step。
- 已在 `src/P2/worker.ts` 增加 `workerCreate` 必填校验：缺少 `type/task/style` 时立即返回可解析错误。
- 已增加 `tests/integration/worker-memory-diagnostic.test.ts`，覆盖“找不到 worker 时写入共享内存诊断键”。
- 已增加 `tests/P3/chainPlanner.test.ts`，覆盖“缺少「做什么」描述时抛出错误”。
- 已增加 `tests/integration/worker.test.ts`，覆盖“缺少 config 时应返回错误”。
- 已运行 `npm test`：20 文件 / 261 测试通过。
- 已同步文档：`src/P2/README.md`、`src/P3/README.md`、`README.md`，补充失败诊断键与空 step 抛错说明。

### 当前剩余缺口
- AC2（模型解析失败）：当前已有 `P1/spawn.test.ts` 模型解析失败测试，但建议再补一条“调度入口触发模型解析失败时不进入空执行”的最小集成覆盖。

### 手动验收命令
- `npm run build`
- `npm test`
- 验证 `worker_start` 失败时调用 `worker_getMemory` 可见 `worker-<workerId>` 诊断键
- 验证 `dteam(action=plan)` 在目标为空时返回错误（而非生成空 worker）

### 收口记录
（待补：完成 AC1~AC6 全部实施后统一收口）

