# 实施-自适应并发

## 基本信息
- ID: 20260601182644-jxih
- 类型: refactor
- 创建时间: 2026-06-01T10:26:44.916Z
- 状态: todo

## 目标
- 为什么: dteam P2 团队模式（src/P2/team.ts:33）使用 mapWithConcurrencyLimit 配合静态 concurrency 参数。复杂任务中最佳并发度会随 CPU 负载、内存压力、任务队列长度动态变化。any 用"冷启动→探索→稳态→过载保护"机制，dteam 没有，会出现"开多了过载/开少了吞吐低"的两难。
- 做什么: 在 dteam 引入自适应并发控制器：基于系统 CPU/内存采样 + 任务吞吐反馈，动态调节 team 模式实际并发度。冷启动保守、探索期递增、稳态期围绕最优值微调、过载保护自动降级。

## 范围## 范围

### 包含

- P0 新增 `adaptiveConcurrency.ts`：采样器（CPU/内存/吞吐）+ 状态机（coldStart/exploring/steady/overload）
- P2/team.ts 集成：默认走 AdaptiveConcurrency，除非显式传 concurrency 字段才用静态值
- 配置：WorkerConfig 新增 `concurrencyMode: "static" | "adaptive"`（默认 adaptive）+ `maxConcurrency` / `minConcurrency` 边界
- 状态机转移：冷启动(1-2 worker)→探索(每次 +1)→稳态(±1 微调)→过载(CPU>85% 或 内存<500MB 时 -1)
- 单元测试：状态机转移、采样器、边界保护

### 排除

- 替换 mapWithConcurrencyLimit 底层（保留 P0/concurrency.ts 工具）
- 全局并发预算（多个 team 共享一个并发池，独立 task）
- LLM 模型自适应（指 worker 内调用的模型选择，不在本 task）

## 验收条件（GWT + 测试）## 验收条件（GWT + 测试）

- [ ] AC1 状态机：coldStart / exploring / steady / overload 4 个状态实现完整
- [ ] AC2 冷启动：默认从 1 个 worker 开始，逐步 +1 直到稳态
- [ ] AC3 过载保护：CPU > 85% 或 内存 < 500MB 时主动 -1
- [ ] AC4 稳态微调：吞吐稳定时 ±1 微调
- [ ] AC5 静态模式兼容：显式传 concurrency 时走原 mapWithConcurrencyLimit，行为不变
- [ ] AC6 边界：minConcurrency / maxConcurrency 不被突破
- [ ] AC7 测试：单测覆盖状态机 + 采样器；`npm test` 不回归
- [ ] AC8 配置：WorkerConfig 新增字段，老调用方不传默认走 adaptive

## 阶段记录
### 探索发现
（待填写）

### 讨论决策
（待填写）

### 执行记录
（待填写）

### 收口记录
（待填写）
