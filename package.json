{
  "name": "pi-dteam",
  "version": "0.5.0",
  "description": "dteam - goal-driven self-growing multi-worker collaboration extension for Pi (summon pool / collective intelligence); 0.6.0 redefinition documented, code migration pending",
  "type": "module",
  "keywords": [
    "pi-package",
    "pi",
    "dteam",
    "workflow",
    "multi-agent",
    "extension"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ssdiwu/pi-dteam.git"
  },
  "files": [
    "index.ts",
    "src/**/*.ts",
    "agents/**/*",
    "README.md",
    "README-zh.md",
    "CHANGELOG.md"
  ],
  "pi": {
    "name": "dteam",
    "extensions": [
      "./index.ts"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-ai": "^0.78.0",
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.7"
  },
  "scripts": {
    "build": "tsc",
    "pretest": "npm run build",
    "test": "vitest run --exclude 'archive/**'",
    "test:watch": "vitest --exclude 'archive/**'"
  }
}
