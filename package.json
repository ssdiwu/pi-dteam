{
  "name": "pi-dteam",
  "version": "0.9.2",
  "description": "dteam - model-tier routing execution layer and evidence-driven main-agent protocol for Pi; fresh T1/T2/T3 workers, structured verification, and bounded fan-out",
  "type": "module",
  "keywords": [
    "pi-package",
    "pi",
    "dteam",
    "workflow",
    "multi-agent",
    "extension"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ssdiwu/pi-dteam.git"
  },
  "files": [
    "index.ts",
    "src/**/*.ts",
    "README.md",
    "README-zh.md",
    "CHANGELOG.md"
  ],
  "pi": {
    "name": "dteam",
    "extensions": [
      "./index.ts"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-ai": "^0.82.1",
    "@earendil-works/pi-coding-agent": "^0.82.1",
    "@earendil-works/pi-tui": "^0.82.1"
  },
  "scripts": {
    "test": "bun test --max-concurrency=1",
    "test:watch": "bun test --watch --max-concurrency=1"
  }
}
