---
title: "共享内存工具"
kind: definition
domain: P1-分子层
status: stable
tags: [共享内存, 工具, 分子层]
created: 2026-05-30
updated: 2026-05-30
---

# 共享内存工具

> **定位**：dteam 的分子层。定义共享内存工具接口。

## 一句话

共享内存工具是 dteam 的**数据共享接口**——提供共享内存的读写能力。

## 工具清单

| 工具 | 功能 | 参数 |
|------|------|------|
| `memory.get` | 从共享内存获取值 | `{ namespace, key }` |
| `memory.set` | 向共享内存设置值 | `{ namespace, key, value, agentId }` |
| `memory.keys` | 列出命名空间下的所有键 | `{ namespace }` |
| `memory.has` | 检查键是否存在 | `{ namespace, key }` |
| `memory.delete` | 删除键 | `{ namespace, key }` |
| `memory.clear` | 清空命名空间 | `{ namespace }` |
| `memory.save` | 保存共享内存到文件 | `{ filepath }` |
| `memory.load` | 从文件加载共享内存 | `{ filepath }` |

## 使用示例

```typescript
// 设置值
await memorySet(ctx, {
  namespace: "task-1",
  key: "result",
  value: { success: true },
  agentId: "worker-0",
});

// 获取值
const result = await memoryGet(ctx, {
  namespace: "task-1",
  key: "result",
});

// 列出所有键
const keys = await memoryKeys(ctx, {
  namespace: "task-1",
});

// 检查键是否存在
const exists = await memoryHas(ctx, {
  namespace: "task-1",
  key: "result",
});

// 删除键
await memoryDelete(ctx, {
  namespace: "task-1",
  key: "result",
});

// 清空命名空间
await memoryClear(ctx, {
  namespace: "task-1",
});

// 保存到文件
await memorySave(ctx, {
  filepath: ".dteam/memory.json",
});

// 从文件加载
await memoryLoad(ctx, {
  filepath: ".dteam/memory.json",
});
```

## 文件位置

- 代码：`src/tools/memory.ts`
