# types/

| 文件 | 内容 |
|---|---|
| `dispatch.ts` | T1/T2/T3、思考强度、模型路由与尝试记录的类型契约 |

0.8 的公开 `dteam` 工具、worker 生命周期、signal 与 parent event 类型位于 [`../runtime/types.ts`](../runtime/types.ts)，不在本目录复制。这里不保留 loop、role 或旧 run/UI 状态类型。
