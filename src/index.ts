/**
 * dteam v1 入口占位
 *
 * 当前状态：等待从第一性原理重新设计。
 * 旧实现已归档到 archive/v0-pre-rewrite/。
 *
 * 设计目标（来自 .doc/）：
 * - task 双层验收（A 层可校验 / B 层人工裁决）
 * - workItems 作为运行态真相
 * - 结构化信号（progress / found / help / blocked）
 * - 半自动 + 人工确认
 *
 * 不再做的事：
 * - 5 层过度分层
 * - 过度抽象
 * - 协议层强、运行态弱的脱节
 *
 * 关联 task：20260603222236-kp82 从零重写 dteam worker 引擎主干
 */

// 占位：v1 重写前 Pi 加载入口不应抛错
export default async function () {
  // noop：等待 v1 设计落地
}
