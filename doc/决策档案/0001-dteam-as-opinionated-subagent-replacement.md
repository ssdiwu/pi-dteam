# dteam 作为有立场的子代理替代层

dteam 的目标不是复刻 `pi-subagents` 的完整功能，而是成为更符合 507 工作流的 subagent / swarm 替代层。它可以研究并吸收 chain file、frontmatter、worktree、worker tree 等完整能力形态，但先按“内部结构优先，外部配置面暂缓”评估：优先让 dteam 自己更清楚、更安全、更可观察，再决定是否暴露为用户可配置入口。
