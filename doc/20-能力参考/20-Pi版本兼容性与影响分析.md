# Pi 版本兼容性与影响分析

> 记录上游 Pi 更新对本项目的直接影响和可改进点，供维护决策参考。
>
> **0.8 状态注记（2026-07-14）**：当前 dteam 是唯一 `dteam` 工具驱动的 T1/T2/T3 fresh worker 执行层，并提供 `/dteam` 管理命令、会话级后台 Worker Manager 和实时 Snapshot；不恢复 Orchestrator、Reporter、`/resume`、`runId` 或跨进程持久化。扩展入口为项目根目录 `index.ts`；0.6/0.7 编排形态只作历史追溯。

---

## 适用范围

本文档当前跟踪的 Pi 更新基线：**v0.82.1（2026-07-25）**。

本轮直接相关变更：
- v0.82.0：`Tool.constrainedSampling`（受约束工具采样）支持严格 JSON Schema（JSON 模式）并按模型能力降级；
- v0.82.1：custom message renderer（自定义消息渲染器）新增 `outputPad`，并更新模型、认证和目录发现；
- 早期的 Project trust（项目信任门控）、缓存可见性、SDK / RPC 与 compaction（压缩）变更仍保留在下方作为历史兼容记录。

---

## 0.82.x 影响评估：✅ 同步开发依赖；无需 runtime 改动

### 受约束报告

`dteam_report` 已使用 `constrainedSampling: { type: "json_schema", strict: "prefer" }`：支持严格模式的模型由 Pi 约束输出，不支持的模型保持普通工具调用。Codex 的严格子集拒绝 `uniqueItems` 与 `oneOf`，因此 provider schema 不携带这两项；重复 activity（活动）与 verification（验证）交叉语义继续由 parser（解析器）校验。此安排与 dteam 的多供应商回退一致，因此不应改为 `require`（强制）。

### 不适用能力

- v0.82.1 的 `outputPad` 只传给 custom message renderer；dteam 当前仅注册 custom tool renderer，不能据此修改渲染代码。
- Claude Opus 5、网关认证和模型目录更新由 Pi 模型配置承载；dteam 不硬编码模型目录，无需代码适配。

### 本轮同步

- 本机 Pi 与 npm 最新版均为 0.82.1；
- 开发依赖同步为 `@earendil-works/pi-ai`、`@earendil-works/pi-coding-agent`、`@earendil-works/pi-tui` 的 `^0.82.1`，使测试解析的类型与实际运行时一致；
- 继续以现有严格报告测试和真实 Pi 冒烟验证该集成。

---


## 影响评估：🟡 可改进 — project_trust 自动信任提升多代理体验

### 变更详情

新增 `project_trust` 扩展事件：

```typescript
pi.on("project_trust", async (event, ctx) => {
  // event.cwd - 当前工作目录
  // ctx: { cwd, mode, hasUI, select, confirm, input, notify }
  if (await ctx.ui.confirm("Trust project?", event.cwd)) {
    return { trusted: "yes", remember: true };
  }
  return { trusted: "undecided" };
});
```

- 仅 **user/global 和 CLI `-e` 扩展** 参与（project-local 扩展在 trust 之后才加载）
- 返回 `"yes"` / `"no"` 则拥有决定权（首个 yes/no 获胜，抑制内置提示）
- `remember: true` 可持久化决定

### 与 dteam 的关系

dteam 是**模型分级 dispatch 执行层**：worker 在调用者 `cwd` 内创建 fresh session，默认 Logical Isolation 不继承扩展；不跨项目切换 cwd、不维护 resume 或 runId。

| 场景 | 与 project_trust 的交互 |
|------|------------------------|
| 当前项目已由 Pi 主会话加载 | dispatch worker 复用该 cwd，不额外改变 trust 决策 |
| 并行 worker | 仅在同一 cwd 创建独立内存 session，不产生跨项目 trust 弹窗 |
| 用户切换到新项目 | 由 Pi 主会话的内置 trust 流程处理，dteam 不拦截 |

### 影响

当前无直接功能缺口。自动信任会扩大安全边界，且 dteam 没有跨项目编排需求，因此保持不监听 `project_trust`。若未来出现明确的跨 cwd dispatch 需求，需先单独 ADR 决策，不能默认放行。

### 示例代码（方案 B — 委托 + 记忆）

```ts
// 在 extension 入口注册
pi.on("project_trust", async (event, ctx) => {
  // dteam 子任务通常访问用户自己的项目，默认倾向信任
  // 但仍给用户一次确认机会，之后记住决定
  if (!ctx.hasUI) return { trusted: "undecided" };

  const confirmed = await ctx.ui.confirm(
    "dteam: 信任此项目目录？",
    `子代理将访问: ${event.cwd}`
  );
  return { trusted: confirmed ? "yes" : "no", remember: confirmed };
});
```

### 优先级

⚪ **当前不做** — 0.7 dispatch 不跨 cwd；只有出现真实跨项目派发需求时才单独评估。

---

## 次要关联

### Cache-hit Visibility（CH）

- Footer 新增缓存命中率显示。
- dteam 的并行 dispatch 调用 LLM，CH 指标有助于调优 prompt 设计和 token 使用效率。
- **无需代码改动**；dteam 没有 `reporter.ts`。

### Compaction Prompt 措辞（#5401）

- dteam 的子代理属于 **non-coding agent** 场景。
- 当 dteam 会话触发 compaction 时，Pi 内部现在会用中性 AI 助手措辞（不再偏 coding）。
- **正面影响**：dteam 的压缩摘要质量应该会有微升。
- **无需代码改动**。

### Skill Prompt 间距（#5371）

- dteam 不以 skill 形式分发，**无影响**。

---

## 版本记录

| 日期 | Pi 基线 | 变更 |
|------|---------|------|
| 2026-07-25 | v0.82.1 | 同步 pi-ai / coding-agent / pi-tui 开发依赖；确认严格报告使用 constrainedSampling 的 prefer 回退，无 runtime 改动 |
| 2026-06-09 | 2026-06-09 发布版 | 初版创建；标记 project_trust 为可改进项 |
