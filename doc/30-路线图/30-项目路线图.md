# 项目路线图

> 单一来源的演进计划。**当前版本 0.4.0**。
>
> 规划窗口：
> - **0.4.1~0.4.9** — patch 增量（minor 不变，bugfix / 小功能）
> - **0.5.0~0.5.5** — minor 跳变（多文件改动，建议单独写方案）
> - **0.5.6+** — 结构性大改
>
> 来源：
> - [设计-v2.md](./设计-v2.md) — 偏离设计产生的 dteam 内部待办
> - [与pi-subagent对比.md](./与pi-subagent对比.md) — 外部借鉴调研的待办
> - [Agent Teams 借鉴实施方案.md](./Agent%20Teams%20借鉴实施方案.md) — 并行任务的文件边界、冲突隔离、可观测性实施方案
>
> **校准**：dteam = 轻量化编排引擎。路线图按 **要做 / 暂不做 / 远期** 三档管理，不是 **做 / 抛弃** 二档。

## dteam 内部待办（设计偏离产生的演进项）

| 对应范围 | 优先级 | 问题 | 解决方向 |
|---------|--------|------|----------|
| 0.4.1 | P1 | 正常聊天区不显示子代实时动态 / 反馈流 | 把 dteam 工具结果升级为 live summary，并和 /dteam 面板共享同一份状态模型 |
| 0.4.1~0.4.3 | P1 | UI 不显示 plan mode | 折叠态标题加 `[chain]` 前缀 |
| 0.4.1~0.4.3 | P2 | planner 规则只能识别中文 | 增加英文关键词正则 |

## 借鉴 oira666/pi-subagent 的待办

### 0.4.1~0.4.3（小改/小配置）

- [ ] **planner 英文关键词正则** — 当前规则只能识别中文（`src/planner.ts:quickRuleBasedPlan`），加几条英文 fallback。
- [ ] **扁平 usage 统计** — 给 `RunResult` 加 `usage: { input, output, cacheRead, cost }` 字段，单层不递归。复盘时知道哪个 step 烧 token。

### 0.4.4~0.4.7（1~2 个文件改动）

- [ ] **frontmatter 角色解析** — 让 `agents/*.md` 的 frontmatter 覆盖 `ROLE_DEFAULTS`，省一张表。`src/session/role-config.ts` 改造点。
- [ ] **steering 广播（多 worker 路由）** — 当前 single point 注入到 `pendingSupplements` Map。team 模式多 worker 并行发 help 时，用户一条消息应该广播给所有等待中的 worker，不只是第一个。

## 暂不做（轻量化原则）

> 保留在路线图里，等痛点变强再升级到"要做"。每一项都标了"暂不做的理由"。

### 借鉴项

- **depth / cycle guard** — dteam 不递归 spawn subagent（in-process 架构）；chain 防重 task 是单独事，需要时单独评估
- **递归 usage tree** — 扁平统计够用；schema 兼容，需要时升级不复杂
- **check / adaptive 改 tool calling** — 正则"通过/pass/✓"够用，引入 tool calling 增加调用成本 + 错误处理

### 内部项

- **递归分解未接入** — brancher 复杂度过大，让 LLM 多轮解决（在 context window 内）
- **check/adaptive 凭正则判断** — 同借鉴项
- **极端大任务可能跑不完** — dteam 轻量化不解决，超大任务应拆为多次 dteam 调用
- **没有持久化** — in-memory 够用，进程崩了重跑

## 远期规划（结构性大改）

> 不是"不做"，是"未来某天再说"。这两项涉及多文件 + 启动开销等结构性改动，需要单独写方案。

- **persistence + resume** — 写盘 + 序列化 `SessionManager` + resume 检测。对方做在 `sessions-subagents/`，可以做在 `.dteam/sessions/`。
- **进程级隔离** — 把 `leaf.execute` 内部的 `createAgentSession` 换成 `spawn('pi', [...])`，子崩不影响主。涉及 `src/session.ts` 重写 + 信号通路（`SignalBus`）改造 + API key 透传策略调整。

## 更新原则

- 完成一项 → 把 `[ ]` 改成 `[x]`（借鉴部分），或在内部待办表里删行
- 范围变更 → 调整"对应范围"列
- 重要新项 → 加到对应范围段
- 暂不做 / 远期 项**不删除**——痛点变强时升级到对应档位
- 路线图变更尽量在独立 commit，方便回顾
