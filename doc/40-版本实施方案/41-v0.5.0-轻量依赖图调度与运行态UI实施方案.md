# v0.5.0 轻量依赖图调度与运行态 UI 实施方案

> **版本归属**：0.5.0  
> **状态**：准备实施  
> **合并来源**：原 `40-Agent-Teams借鉴实施方案.md` + 原 `43-v0.5-可观测性与轻量协作实施方案.md`  
> **一句话目标**：让 dteam 具备最小 SwarmFlow / MiMo 式编排确定性：基于文件边界和轻量依赖图生成可解释 worker 批次，并一次性重做运行态 UI。

## 1. 目标排序

507 对 0.5.0 的目标排序是：

1. **C**：最小 SwarmFlow / MiMo 式编排确定性
2. **A**：多 worker 协作可见、可解释、可控
3. **B**：知道什么时候该并行，什么时候该避让

因此 0.5.0 不是单纯 UI 小修，也不是重型流水线，而是：

> **轻量依赖图调度 + 运行态 UI 重做。**

## 2. 设计来源

| 来源 | 借鉴 | 不照搬 |
|---|---|---|
| SwarmFlow | 编排和智能分离：能确定的协作关系交给系统执行 | 不引入 `workflow.py`，不做通用 workflow 平台 |
| MiMo Code | 编排结构化、旁路 verifier / writer 思想 | 不做固定 6 阶段流水线，不做 Max Mode |
| rpiv-todo | 简洁任务树 UI、完成项保留、删除线弱化完成项 | 不集成 todo 工具，不引入 blockedBy 模型 |
| pi-subagents | async widget、grouped result、child safety、自然语言可用性 | 不开放自定义 agent / chain 文件体系 |
| Agent Teams | 文件边界、冲突预检、并行解释 | 不做 tmux、worktree、文件锁、自动 merge |

## 3. 非目标

0.5.0 不做：

- 固定 `review → TDD → verify → merge` 流水线
- workflow 脚本 API
- 自定义角色系统
- 文件锁
- Git worktree
- 自动 merge / rebase
- TypeScript compiler API / Babel AST 级别依赖分析
- 跨语言依赖图
- 持久化 `.dteam/sessions/`
- 恢复后台 `onUpdate`

## 4. 系统分层定义

Q8 中的“系统”必须明确：

> **系统 = dteam runtime（运行时）的确定性代码层，不是主 LLM，也不是 worker LLM。**

| 层 | 职责 | 是否 LLM |
|---|---|:---:|
| 主 LLM | 判断是否调用 dteam，提供 goal | ✅ |
| planner | 生成 `ExecutionPlan`，给出初始 `step.files` | ✅ + 规则 |
| dteam runtime | 补全文件边界、建依赖图、做 preflight、生成 batch | ❌ |
| worker | 执行具体 step | ✅ |
| UI / report | 展示状态、批次、冲突、延后原因 | ❌ |

0.5.0 的核心是把“编排确定性”下沉到 dteam runtime，而不是要求 leader agent 每轮临场记住。

## 5. 状态存储策略

0.5.0 采用三层状态：

| 状态 | 存放位置 | 说明 |
|---|---|---|
| 运行中热状态 | 内存：`activeRuns` / `uiStore` / `SignalBus` / `RunsStore` | 高频变化，不写 session |
| plan / fileGraph / scheduling checkpoint | `pi.appendEntry()` | 写入 Pi session，不进入 LLM context |
| 最终报告 | `dteam-report.details` | 用户展开回溯 dteam 做了什么、怎么调度 |

示例：

```ts
pi.appendEntry("dteam-scheduling", {
  runId,
  goal,
  plan,
  fileGraph,
  batches,
  conflicts,
  createdAt,
})
```

原则：

- 不自己写 `.dteam/sessions/`
- 不把高频 progress 写入 `appendEntry`
- 完成态 UI 快照保留在内存，直到下一次 dteam run 才清理

## 6. `PlanStep.files` 契约

`PlanStep.files` 表示该 step 预计会读 / 改的关键文件集合。

```ts
interface PlanStep {
  role: RoleName
  task: string
  strategy: Strategy
  files?: string[]
}
```

约定：

1. planner 尽量产出 `files`
2. `files` 可以不完整，但不能故意乱填
3. 缺失 `files` 不阻塞执行，标记为 unknown boundary
4. dteam runtime 会基于文件系统补全 import 依赖
5. `files` 不是文件锁，也不是精确写集

## 7. 轻量依赖图

0.5.0 的依赖图是轻量文本扫描，不做编译器级分析。

### 支持

| 能力 | 做法 |
|---|---|
| JS / TS import 图 | 正则扫描 `import` / `export from` / `require` |
| 相对路径解析 | `./x` / `../x` 解析到候选文件 |
| index 文件 | 支持 `dir/index.ts` / `dir/index.tsx` |
| shared files | `package.json` / lockfile / config / central index 标记高风险 |
| unknown | 无法解析则标记 unknown，不阻塞 |

### 不支持

| 不做 | 原因 |
|---|---|
| TypeScript compiler API | 太重 |
| Babel AST | 太重 |
| 跨语言依赖图 | 超出 0.5.0 |
| 动态 import 精准解析 | 首版不做 |
| monorepo package graph | 远期 |

## 8. 冲突分类

| 等级 | 条件 | 行为 |
|---|---|---|
| hard conflict | 两个 step 显式声明同一文件 | 自动拆批 |
| shared conflict | 涉及 `package.json` / lockfile / config / central index | 自动拆批 |
| dependency edge | A 文件被 B import | 按依赖方向拆批：被依赖方先，依赖方后 |
| unknown boundary | step 无 `files` 或无法解析 | 不阻塞，UI / report 标 unknown |
| no conflict | 无交集、无依赖 | 同批并行 |

依赖方向规则：

```text
auth.ts imports user.ts

user.ts = 被依赖方
 auth.ts = 依赖方

调度：改 user.ts 的 step 先跑；改 auth.ts 的 step 后跑。
```

## 9. batch 生成规则

输入：`ExecutionPlan.steps` + file graph。  
输出：`SchedulingPlan`。

```ts
interface SchedulingPlan {
  batches: Array<{
    index: number
    stepIndexes: number[]
    reason: string
  }>
  conflicts: Array<{
    type: "hard" | "shared" | "dependency" | "unknown"
    stepIndexes: number[]
    files?: string[]
    reason: string
  }>
}
```

规则：

1. `solo` / `chain` 不改组织语义，只补充 UI 信息
2. `team` 才做 preflight scheduling
3. 同批出现 hard/shared/dependency 冲突时，后者延后到后续 batch
4. unknown 不阻塞，但在 UI / report 中展示
5. batch 内仍并行，batch 间串行
6. 不把 plan 强制改写成 `chain`，只改变 team 的执行批次

## 10. UI 重做

当前 UI 的问题：

- 折叠态层级不清，running / done / failed 不明显
- 展开态 tab 过工程化，像 debug panel
- 输出太吵，信息密度不稳定
- 完成后直接清空，不利于回溯
- 缺少 batch / conflict / dependency 解释

0.5.0 一次性重做运行态 UI。

### 10.1 UI 总原则

要做：

- 单一清晰标题
- 稳定状态符号
- running 高亮
- done 删除线 + dim
- failed error
- delayed warning
- batch 和 dependency 原因可见
- 输出克制：widget 只显示 running 最新一行，expanded 每个 worker 最多 2 行
- 完成态保留，直到下一次 dteam run 才清理

不要做：

- 不要大块边框
- 不要每个 worker 一个 noisy tab
- 不要每个 worker 都显示长输出
- 不要过多 emoji
- 不要 debug panel 风格

### 10.2 运行态 widget

用途：run 进行中自动显示，尽量像 rpiv-todo 的任务树。

示例：

```text
● dteam [team] 2/5 · 01:24
├ ✓ explore  scan src/ui
├ ◐ build    panel shell        batch 1/3
│   ⎿ editing src/ui/panel.ts
├ ↳ build    status model       delayed: panel.ts conflict
├ ○ check    verify UI states
└ ○ close    summarize
────────────────────────────────────────
```

状态符号：

| 符号 | 状态 | 颜色 |
|---|---|---|
| `○` | pending | muted |
| `◐` | running | accent |
| `✓` | done | muted + 删除线 |
| `✕` | failed | error |
| `↳` | delayed | warning |
| `?` | unknown | muted / warning 视情况 |

### 10.3 `/dteam` 展开态

Q10 决策：**保留 tab**，避免不同内容全部堆在一起。  
但 tab 改成内容型 tab，不再是当前“每个 worker 一个 tab”。

建议 tabs：

| Tab | 内容 |
|---|---|
| `0 概览` | goal、mode、总进度、当前 batch、关键 warning |
| `1 批次` | batches、延后原因、dependency / conflict |
| `2 Workers` | worker 树、状态、files、最新输出 |
| `3 信号` | found / progress / help / blocked 聚合 |
| `4 报告` | 完成后 final summary；运行中显示待完成 |

展开态示例：

```text
● dteam [team] 2/5 · running · 01:24
[0 概览] [1 批次] [2 Workers] [3 信号] [4 报告]

Batch 1 · running
├ ✓ explore  src/ui, src/orchestrator
├ ◐ build    panel shell
│   files: src/ui/panel.ts, src/ui/store.ts
│   now: rendering compact worker tree
└ ↳ build    status model
    delayed: depends on src/ui/store.ts

/dteam N 切换 · /dteam close
```

### 10.4 完成态保留

完成后不立即清空 UI：

1. `uiStore.finishRun()` 后保留最后快照
2. widget 显示完成态摘要
3. `/dteam` 仍可展开查看上一轮 dteam 做了什么、怎么做的
4. 只有下一次 `dteam(action="run")` 开始时清理上一轮完成态

完成态示例：

```text
✓ dteam [team] 5/5 · done · 03:12
├ ✓ explore  scan src/ui
├ ✓ build    panel shell
├ ✓ build    scheduler preflight
├ ✓ check    tests passed
└ ✓ close    summary
────────────────────────────────────────
```

### 10.5 report

`dteam-report.details` 增加：

```ts
{
  runId,
  status,
  summary,
  mode,
  reason,
  plan,
  fileGraph,
  scheduling,
  steps: [{ role, status, strategy, task, files, output }]
}
```

折叠态：

```text
✓ dteam [team] · 5/5 完成 · 1 个冲突已避让
```

展开态：显示 batch、conflicts、steps。

## 11. 代码放置位置

0.5.0 不做大目录重组，但需要新增少量专用模块，避免把调度逻辑塞进 `orchestrator.ts`。

```text
src/
├── scheduler/                         ← 新增：轻量依赖图与 batch 调度
│   ├── file-graph.ts                  ← import / require 文本扫描 + 文件解析
│   ├── preflight.ts                   ← conflict 分类 + batch 生成
│   ├── shared-files.ts                ← shared file patterns
│   └── index.ts                       ← 统一导出
├── ui/
│   ├── store.ts                       ← 扩展 UIState：mode / reason / scheduling / completed snapshot
│   ├── panel.ts                       ← 重做 widget + /dteam tabs
│   └── helpers.ts                     ← 状态符号 / 颜色策略 / 删除线辅助
├── reporter.ts                        ← 增加 plan / scheduling 汇报入口
├── orchestrator.ts                    ← team 执行前调用 scheduler，按 batches 执行
├── tools.ts                           ← 扩展 SchedulingPlan / FileGraph / RunResult 类型
└── config.ts                          ← shared file patterns / scheduler 配置

tests/
├── scheduler-file-graph.test.ts        ← 新增
├── scheduler-preflight.test.ts         ← 新增
├── ui-store.test.ts                    ← 扩展
├── orchestrator.test.ts                ← 扩展 batch 执行测试
└── index-background-run.test.ts        ← 扩展完成态保留 / report details
```

不新增 `doc/` 子目录；本文件就是 0.5.0 的实施权威。

## 12. 阶段划分

| 阶段 | 目标 | 产出 | 验收 |
|---|---|---|---|
| Phase 0 | 契约冻结 | 类型、状态、配置边界定稿 | ✅ 已完成，build/test 通过 |
| Phase 1 | 轻量 file graph | 能解析相对 import / require | 新增 file graph 单测通过 |
| Phase 2 | preflight scheduling | team 显式冲突自动拆批 | scheduler 单测通过 |
| Phase 3 | orchestrator 接入 | team 按 batches 执行 | orchestrator 单测通过 |
| Phase 4 | session checkpoint | appendEntry + report details | 后台 run 测试通过 |
| Phase 5 | widget 重做 | 运行态任务树 | UI store / panel 测试通过 |
| Phase 6 | `/dteam` tabs 重做 | 概览 / 批次 / Workers / 信号 / 报告 | 手动验证通过 |
| Phase 7 | 文档与收口 | README / API / CHANGELOG 同步 | 全量测试通过 |

## 13. Phase 0：契约冻结 checklist

目标：先把数据结构定稳，后续实现不反复改字段。

- [x] 在 `src/tools.ts` 增加 `FileGraph` 类型
- [x] 在 `src/tools.ts` 增加 `SchedulingPlan` 类型
- [x] 在 `src/tools.ts` 增加 `SchedulingConflict` 类型
- [x] 在 `RunResult` 增加 `fileGraph?: FileGraph`
- [x] 在 `RunResult` 增加 `scheduling?: SchedulingPlan`
- [x] 在 `StepResult` 或 UI 映射中保留 `files?: string[]`
- [x] 在 `src/ui/store.ts` 增加 `mode?: ExecMode`
- [x] 在 `src/ui/store.ts` 增加 `planReason?: string`
- [x] 在 `src/ui/store.ts` 增加 `scheduling?: SchedulingPlan`
- [x] 在 `src/config.ts` 增加 scheduler 配置：shared file patterns、支持扩展名、最大扫描文件数
- [x] 更新类型测试或新增最小类型测试
- [x] `npm run build` 通过

完成标准：类型可编译，运行行为不变。✅

## 14. Phase 1：轻量 file graph checklist

目标：dteam runtime 能根据 planner 给的 `step.files` 补轻量依赖图。

- [ ] 新增 `src/scheduler/file-graph.ts`
- [ ] 支持解析静态 `import x from "./x"`
- [ ] 支持解析 `export * from "./x"` / `export { x } from "./x"`
- [ ] 支持解析 `require("./x")`
- [ ] 支持 `.ts` / `.tsx` / `.js` / `.jsx` / `.mjs` / `.cjs`
- [ ] 支持目录 `index.ts` / `index.tsx` / `index.js`
- [ ] 支持相对路径规范化为相对项目根路径
- [ ] 缺文件时标记 unresolved，不 throw
- [ ] 超出扫描限制时标记 truncated，不阻塞
- [ ] 新增 `tests/scheduler-file-graph.test.ts`
- [ ] 覆盖：相对 import、index 文件、require、缺文件、非 JS/TS 文件
- [ ] `npm test -- scheduler-file-graph` 通过

完成标准：给定 step files，能生成稳定 `FileGraph`，失败只降级为 unknown / unresolved。

## 15. Phase 2：preflight scheduling checklist

目标：在不引入文件锁的前提下，让 team 模式避开显式冲突。

- [ ] 新增 `src/scheduler/shared-files.ts`
- [ ] 配置 shared file patterns：`package.json`、lockfile、`tsconfig.json`、`vitest.config.*`、根 `index.ts`
- [ ] 新增 `src/scheduler/preflight.ts`
- [ ] hard conflict：两个 step 显式声明同一文件 → 自动拆批
- [ ] shared conflict：共享文件相关 step → 自动拆批
- [ ] dependency edge：被依赖文件 step 先，依赖方 step 后
- [ ] unknown boundary：缺失 files → 不阻塞，只记录 warning
- [ ] 无冲突 step 保持同批并行
- [ ] 输出 `SchedulingPlan.batches`
- [ ] 输出 `SchedulingPlan.conflicts`
- [ ] 输出每个 delayed step 的 `delayedBecause`
- [ ] 新增 `tests/scheduler-preflight.test.ts`
- [ ] 覆盖 hard conflict
- [ ] 覆盖 shared conflict
- [ ] 覆盖 dependency direction
- [ ] 覆盖 unknown 不阻塞
- [ ] 覆盖无冲突保持同批
- [ ] `npm test -- scheduler-preflight` 通过

完成标准：同一组 steps 多次输入生成相同 batches，调度可解释。

## 16. Phase 3：orchestrator 接入 checklist

目标：让 `team` 按 preflight 后的 batches 执行，但不改变 `solo` / `chain` 语义。

- [ ] `orchestrator.ts` 在 plan 后构建 file graph
- [ ] `team` 模式执行前调用 preflight scheduler
- [ ] `solo` / `chain` 只附带 scheduling 信息，不改执行顺序
- [ ] batch 内继续 `Promise.all`
- [ ] batch 间串行
- [ ] `reporter.addStrategy` 记录被延后的 step 和原因
- [ ] `RunResult` 返回 `fileGraph` 和 `scheduling`
- [ ] `taskPool` 状态与 batch 执行保持一致
- [ ] 扩展 `tests/orchestrator.test.ts`
- [ ] 覆盖 team 冲突拆批后按批执行
- [ ] 覆盖 chain 不被 scheduler 改写
- [ ] 覆盖 unknown files 不阻塞 team
- [ ] `npm test -- orchestrator` 通过

完成标准：调度语义变化只发生在 `team` 的 batch 层，不把 plan 改写成 chain。

## 17. Phase 4：session checkpoint 与 report details checklist

目标：完成后可回溯，执行中关键 checkpoint 可跟随 Pi session 保存。

- [ ] 在 dteam run context 中提供 `appendEntry` 包装函数（便于测试 mock）
- [ ] plan 完成后写 `pi.appendEntry("dteam-plan", ...)`
- [ ] scheduling 完成后写 `pi.appendEntry("dteam-scheduling", ...)`
- [ ] 不写高频 progress / output 到 appendEntry
- [ ] `dteam-report.details` 增加 `plan`
- [ ] `dteam-report.details` 增加 `fileGraph`
- [ ] `dteam-report.details` 增加 `scheduling`
- [ ] `dteam-report.details.steps[]` 增加 `task` / `strategy` / `files`
- [ ] 扩展 `tests/index-background-run.test.ts`
- [ ] 覆盖 report details 包含 scheduling
- [ ] 覆盖后台 run 仍不调用 `onUpdate`
- [ ] `npm test -- index-background-run` 通过

完成标准：用户展开最终报告能看到 dteam 怎么计划、怎么拆批、为什么延后。

## 18. Phase 5：运行态 widget 重做 checklist

目标：运行态自动显示一个像 rpiv-todo 一样清爽的任务树。

- [ ] `uiStore` 支持完成态保留，不在 finish 时 reset
- [ ] 下一次 `dteam(action="run")` 开始前清理上一轮完成态
- [ ] widget 标题显示 `● dteam [mode] done/total · duration`
- [ ] pending 显示 `○` + muted
- [ ] running 显示 `◐` + accent
- [ ] done 显示 `✓` + muted + 删除线
- [ ] failed 显示 `✕` + error
- [ ] delayed 显示 `↳` + warning
- [ ] unknown 显示 `?` + muted / warning
- [ ] widget 只显示 running worker 最新一行输出
- [ ] done worker 不显示输出
- [ ] 行数超限时优先保留 running / failed / delayed
- [ ] 空态自动隐藏
- [ ] 扩展 UI 测试或新增 panel render 测试
- [ ] 手动对比 rpiv-todo 截图，确认视觉接近“轻任务树”而不是 debug panel

完成标准：不打开 `/dteam` 时，用户也能看懂当前 dteam 在干什么。

## 19. Phase 6：`/dteam` tabs 重做 checklist

目标：保留 tab，但从“worker tab”改成“内容 tab”。

- [ ] 移除当前每个 worker 一个 tab 的策略
- [ ] 新增固定 tab：`0 概览`
- [ ] 新增固定 tab：`1 批次`
- [ ] 新增固定 tab：`2 Workers`
- [ ] 新增固定 tab：`3 信号`
- [ ] 新增固定 tab：`4 报告`
- [ ] `0 概览` 显示 goal、mode、summary、当前 batch、关键 warning
- [ ] `1 批次` 显示 batches、delayedBecause、conflicts
- [ ] `2 Workers` 显示 worker 树、状态、files、最多 2 行输出
- [ ] `3 信号` 聚合 found / progress / help / blocked
- [ ] `4 报告` 运行中显示待完成，完成后显示 final summary
- [ ] `/dteam N` 切 tab 继续可用
- [ ] `/dteam close` 继续可用
- [ ] 完成后 `/dteam` 仍能查看上一轮结果
- [ ] 下一次 run 开始后旧完成态清理

完成标准：展开态信息分区清楚，不把所有内容堆在一个页面，也不再像 debug 面板。

## 20. Phase 7：文档与示例 checklist

- [ ] 更新 `README.md` 的 0.5.0 说明
- [ ] 更新 `doc/10-架构与运行/10-系统架构.md`
- [ ] 更新 `doc/10-架构与运行/12-API参考.md`
- [ ] 更新 `doc/30-路线图/30-项目路线图.md`
- [ ] 更新 `CHANGELOG.md`
- [ ] 给出 3 个自然语言使用示例：简单任务不调 dteam、复杂任务调 dteam、team 并行避让
- [ ] 补最终验收记录

完成标准：文档能解释 0.5.0 的行为，不需要读代码才能理解。

## 21. MVP 验收标准

0.5.0 最小可用必须满足：

- [ ] `team` 模式下两个 step 显式声明同一文件时，会自动拆到不同 batch
- [ ] dependency edge 能按方向调度：被依赖方先，依赖方后
- [ ] unknown boundary 不阻塞执行，但 UI / report 可见
- [ ] widget 显示清晰任务树
- [ ] `/dteam` 完成后仍可回溯上一轮结果
- [ ] 下一次 dteam run 开始时清理上一轮完成态
- [ ] `dteam-report.details` 包含 `scheduling`
- [ ] `npm run build` 通过
- [ ] `npm test` 通过

## 22. 完整验收标准

Phase 7 结束时必须满足：

- [ ] Phase 0 ~ Phase 7 checklist 全部完成
- [ ] 新增 scheduler 单测覆盖 hard/shared/dependency/unknown/no conflict
- [ ] 现有 267 个测试继续通过
- [ ] 新增测试通过，总测试数更新到 CHANGELOG 或验收记录
- [ ] 手动验证 solo / chain / team 三种 mode
- [ ] 手动验证同文件冲突拆批
- [ ] 手动验证 import 依赖方向拆批
- [ ] 手动验证完成态保留和下一次 run 清理
- [ ] 手动验证 `/dteam` 五个 tabs
- [ ] 手动验证 report 展开可回溯 plan / batches / conflicts
- [ ] 代码改动按阶段拆 commit，每个 commit 只做一件事

## 23. 风险与降级

| 风险 | 降级策略 |
|---|---|
| `files` 不准 | unknown 不阻塞；只对明确冲突拆批 |
| import scanner 漏判 | 保留 warning，不做强保证 |
| 自动拆批导致变慢 | UI / report 必须说明延后原因 |
| UI 信息过多 | widget 克制，详情放 `/dteam` tabs |
| appendEntry 过多 | 只写 plan / scheduling checkpoint，不写高频 progress |
| 依赖图复杂度膨胀 | 坚持文本扫描，不引入 AST / compiler API |

## 24. 版本结论

0.5.0 的成功标准：

> dteam 能在后台跑多个 worker；runtime 能基于轻量依赖图生成可解释 batch；UI 清楚展示谁在跑、谁被延后、为什么延后、做完后如何回溯；同时保持 dteam 轻量，不变成重型工作流平台。
